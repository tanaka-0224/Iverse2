# 技術構成

## 概要
本プロジェクトは、React + TypeScript + Vite を使用したモダンなWebアプリケーションです。バックエンドにはSupabaseを採用し、リアルタイムデータベースと認証機能を提供しています。

## フロントエンド

### コア技術

#### React
- **バージョン**: 18.3.1
- **用途**: UIフレームワーク
- **主な機能**:
  - コンポーネントベースのアーキテクチャ
  - Hooks API（useState, useEffect, カスタムフック）
  - StrictModeによる開発時の厳格なチェック

#### React DOM
- **バージョン**: 18.3.1
- **用途**: ReactコンポーネントのDOMレンダリング

### ビルドツール・開発環境

#### Vite
- **バージョン**: 5.4.2
- **用途**: ビルドツール・開発サーバー
- **主な機能**:
  - 高速なHMR（Hot Module Replacement）
  - ESモジュールベースのビルド
  - 最適化された本番ビルド
- **設定**: `vite.config.ts`
  - Reactプラグイン統合
  - lucide-reactの最適化設定

#### TypeScript
- **バージョン**: 5.5.3
- **用途**: 型安全なJavaScript開発
- **設定ファイル**:
  - `tsconfig.json`: プロジェクト全体の設定
  - `tsconfig.app.json`: アプリケーション用設定
  - `tsconfig.node.json`: Node.js環境用設定

### スタイリング

#### Tailwind CSS
- **バージョン**: 3.4.1
- **用途**: ユーティリティファーストのCSSフレームワーク
- **主な機能**:
  - レスポンシブデザイン
  - カスタムテーマ設定
  - パージングによる最適化
- **設定**: `tailwind.config.js`

#### PostCSS
- **バージョン**: 8.4.35
- **用途**: CSS変換ツール
- **設定**: `postcss.config.js`
  - Tailwind CSSプラグイン
  - Autoprefixer統合

#### Autoprefixer
- **バージョン**: 10.4.18
- **用途**: ベンダープレフィックスの自動付与

### UIライブラリ

#### Lucide React
- **バージョン**: 0.344.0
- **用途**: アイコンライブラリ
- **主な使用箇所**:
  - ナビゲーションアイコン
  - UI要素の装飾
  - アクションボタン

## バックエンド・データベース

### Supabase
- **パッケージ**: @supabase/supabase-js
- **バージョン**: 2.57.4
- **用途**: BaaS（Backend as a Service）
- **主な機能**:
  - 認証システム（Email/Password認証）
  - PostgreSQLデータベース
  - リアルタイムデータ同期
  - Row Level Security（RLS）
- **実装**:
  - `src/lib/supabase.ts`: Supabaseクライアント設定
  - `src/hooks/useAuth.ts`: 認証フック
  - デモモード対応（Supabase未設定時のフォールバック）

## 開発ツール

### リンター・コード品質

#### ESLint
- **バージョン**: 9.9.1
- **設定**: `eslint.config.js`
- **使用プラグイン**:
  - `@eslint/js`: 推奨ルールセット
  - `eslint-plugin-react-hooks`: React Hooksのルール
  - `eslint-plugin-react-refresh`: Fast Refresh対応
  - `typescript-eslint`: TypeScript用ルール

#### TypeScript ESLint
- **バージョン**: 8.3.0
- **用途**: TypeScriptコードの静的解析

### その他の開発依存関係

- **@vitejs/plugin-react**: 4.3.1 - Vite用Reactプラグイン
- **@types/react**: 18.3.5 - React型定義
- **@types/react-dom**: 18.3.0 - React DOM型定義
- **globals**: 15.9.0 - ESLint用グローバル変数定義

## プロジェクト構造

```
src/
├── components/          # Reactコンポーネント
│   ├── account/        # アカウント関連
│   ├── auth/           # 認証関連
│   ├── chat/           # チャット機能
│   ├── navigation/     # ナビゲーション
│   ├── posts/          # 投稿機能
│   ├── recommendations/# おすすめ機能
│   └── ui/             # UIコンポーネント
├── hooks/              # カスタムフック
│   ├── useAuth.ts      # 認証フック
│   └── useProfile.ts   # プロフィールフック
├── lib/                # ライブラリ・ユーティリティ
│   ├── supabase.ts     # Supabase設定
│   ├── autoMessage.ts  # 自動メッセージ機能
│   └── demoBoards.ts   # デモボード機能
├── types/              # TypeScript型定義
│   └── database.types.ts # データベース型定義
└── constants/          # 定数定義
    └── messages.ts     # メッセージ定数
```

## 主要な技術パターン

### カスタムフック
- `useAuth`: 認証状態管理
- `useProfile`: プロフィール情報管理

### 状態管理
- React Hooks（useState, useEffect）によるローカル状態管理
- Supabaseのリアルタイムサブスクリプション

### ルーティング
- コンポーネントベースの画面切り替え
- ボトムナビゲーションによる画面遷移

### 認証フロー
- Supabase Authによる認証
- デモモード対応（開発・テスト用）
- セッション管理と自動同期

### PWA（Progressive Web App）対応

#### 現在の状況
- **PWA対応: 未実装**
- manifest.json: なし
- Service Worker: なし
- オフライン機能: なし
- インストール機能: なし

#### 実装予定
PWA対応は将来の機能拡張として計画されています（技術パターンの進化セクション参照）。

## 環境変数

- `VITE_SUPABASE_URL`: SupabaseプロジェクトURL
- `VITE_SUPABASE_ANON_KEY`: Supabase匿名キー

## ビルド・実行コマンド

```bash
# 開発サーバー起動
npm run dev

# 本番ビルド
npm run build

# ビルドプレビュー
npm run preview

# リンター実行
npm run lint
```

## ブラウザサポート

- モダンブラウザ（Chrome, Firefox, Safari, Edge）
- Autoprefixerによる古いブラウザ対応

---

## 技術パターンの進化予測

プロジェクトが発展するにつれて、以下のような技術パターンの変化が予想されます。

### 1. 状態管理の進化

#### 現在のパターン
- **ローカル状態管理**: 各コンポーネントで`useState`と`useEffect`を使用
- **プロップドリリング**: 状態を親から子へ手動で受け渡し
- **グローバル状態なし**: アプリケーション全体の状態管理が分散

#### 発展予測

**フェーズ1: カスタムフックの拡充**
- データフェッチングロジックをカスタムフックに集約
  - `useBoards`: ボード一覧の取得・管理
  - `useMessages`: メッセージの取得・送信
  - `useNotifications`: 通知の取得・更新
- ビジネスロジックとUIロジックの分離

**フェーズ2: 軽量状態管理ライブラリの導入**
- **Zustand** または **Jotai** の導入
  - グローバル状態の一元管理
  - パフォーマンス最適化（不要な再レンダリングの防止）
  - 型安全性の向上
- 認証状態、ユーザープロフィール、通知状態などの共有状態を管理

**フェーズ3: サーバー状態管理の導入**
- **TanStack Query (React Query)** の導入
  - データフェッチングの自動キャッシング
  - バックグラウンドでの自動再取得
  - 楽観的更新（Optimistic Updates）
  - エラーハンドリングとリトライロジックの統一

### 2. データフェッチングの進化

#### 現在のパターン
- 各コンポーネント内で直接Supabaseクエリを実行
- 手動でのローディング状態管理
- エラーハンドリングが個別実装
- キャッシングなし（毎回API呼び出し）

#### 発展予測

**フェーズ1: データフェッチング層の抽象化**
- `src/lib/api/` ディレクトリの作成
  - `boards.ts`: ボード関連のAPI関数
  - `messages.ts`: メッセージ関連のAPI関数
  - `users.ts`: ユーザー関連のAPI関数
- 統一されたエラーハンドリング
- 型安全なAPI関数の実装

**フェーズ2: React Queryの導入**
```typescript
// 例: useBoards フック
const { data, isLoading, error, refetch } = useQuery({
  queryKey: ['boards', userId],
  queryFn: () => fetchBoards(userId),
  staleTime: 30000, // 30秒間キャッシュ
});
```

**フェーズ3: リアルタイム更新の最適化**
- SupabaseリアルタイムサブスクリプションとReact Queryの統合
- 自動的なキャッシュ無効化と再取得
- オフライン対応の検討

### 3. ルーティングの進化

#### 現在のパターン
- コンポーネントベースの手動ルーティング（`App.tsx`のswitch文）
- URLと状態の非同期（ブラウザの戻る/進むボタンが機能しない）
- 深いリンクのサポートなし

#### 発展予測

**フェーズ1: React Routerの導入**
- **React Router v6** の導入
  - URLベースのルーティング
  - ブラウザ履歴のサポート
  - 深いリンクの実現
  - コード分割と遅延読み込み

**フェーズ2: ルート保護の実装**
- 認証が必要なルートの保護
- ローディング状態の管理
- リダイレクトロジックの統一

**フェーズ3: ネストされたルーティング**
- タブナビゲーションとURLルーティングの統合
- モーダルとルートの統合

### 4. エラーハンドリングの進化

#### 現在のパターン
- 各コンポーネントで個別にtry-catchブロック
- エラー表示が統一されていない
- エラーログの管理が分散

#### 発展予測

**フェーズ1: エラーバウンダリの実装**
- React Error Boundaryの導入
- グローバルエラーハンドリング
- エラーUIの統一

**フェーズ2: エラー監視サービスの導入**
- **Sentry** または **LogRocket** の導入
- 本番環境でのエラー追跡
- パフォーマンス監視

**フェーズ3: ユーザーフレンドリーなエラー表示**
- エラーメッセージの国際化
- リトライ機能の統一
- オフライン状態の検知と表示

### 5. 型安全性の向上

#### 現在のパターン
- TypeScriptを使用しているが、一部`any`型が存在
- データベース型定義はあるが、APIレスポンスの型が不完全

#### 発展予測

**フェーズ1: 厳格な型設定**
- `tsconfig.json`で`strict: true`を有効化
- `any`型の排除
- 型ガード関数の実装

**フェーズ2: ランタイム型検証**
- **Zod** の導入
  - APIレスポンスのバリデーション
  - フォーム入力の検証
  - 型安全な環境変数の管理

**フェーズ3: 型生成の自動化**
- Supabase型定義の自動更新
- APIスキーマからの型生成

### 6. パフォーマンス最適化

#### 現在のパターン
- コード分割なし
- 画像最適化なし
- メモ化の使用が限定的

#### 発展予測

**フェーズ1: コード分割と遅延読み込み**
- React.lazy()とSuspenseの使用
- ルートベースのコード分割
- コンポーネントの遅延読み込み

**フェーズ2: 画像最適化**
- **Vite Image Optimization** または **Cloudinary** の導入
- レスポンシブ画像の実装
- 遅延読み込み（lazy loading）

**フェーズ3: バンドルサイズの最適化**
- バンドルアナライザーの導入
- 不要な依存関係の削除
- Tree shakingの最適化

### 7. テスト戦略の導入

#### 現在のパターン
- テストフレームワークなし

#### 発展予測

**フェーズ1: 単体テストの導入**
- **Vitest** の導入（Viteとの統合が容易）
- カスタムフックのテスト
- ユーティリティ関数のテスト

**フェーズ2: コンポーネントテスト**
- **React Testing Library** の導入
- ユーザーインタラクションのテスト
- アクセシビリティテスト

**フェーズ3: E2Eテスト**
- **Playwright** または **Cypress** の導入
- 主要なユーザーフローのテスト
- CI/CDパイプラインへの統合

### 8. 開発体験の向上

#### 現在のパターン
- 基本的なESLint設定
- フォーマッターなし

#### 発展予測

**フェーズ1: コードフォーマッターの導入**
- **Prettier** の導入
- 自動フォーマットの設定
- Git hooks（Husky）での自動フォーマット

**フェーズ2: 開発ツールの拡充**
- **Storybook** の導入（UIコンポーネントの開発・テスト）
- **React DevTools** の活用
- デバッグツールの整備

**フェーズ3: CI/CDパイプライン**
- GitHub Actionsの設定
- 自動テストの実行
- 自動デプロイメント

### 9. アクセシビリティの向上

#### 現在のパターン
- 基本的なHTMLセマンティクス
- アクセシビリティの考慮が限定的

#### 発展予測

**フェーズ1: ARIA属性の追加**
- キーボードナビゲーションの改善
- スクリーンリーダー対応
- フォーカス管理の改善

**フェーズ2: アクセシビリティテスト**
- **axe-core** の導入
- 自動アクセシビリティチェック
- WCAG準拠の確認

### 10. モバイル対応の強化

#### 現在のパターン
- レスポンシブデザイン（Tailwind CSS）
- **PWA機能なし**（未実装）
  - manifest.json: なし
  - Service Worker: なし
  - オフライン機能: なし
  - インストール機能: なし

#### 発展予測

**フェーズ1: PWAの実装**
- **vite-plugin-pwa** の導入
  - Service Workerの自動生成
  - manifest.jsonの自動生成
  - オフライン機能の実装
  - キャッシュ戦略の設定
- インストール可能なアプリとしての機能
  - ホーム画面への追加
  - アプリのような体験
- プッシュ通知の検討（将来）

**フェーズ2: ネイティブアプリの検討**
- **React Native** または **Capacitor** の検討
- コードベースの共有
- ネイティブ機能の活用

#### PWA実装時の技術スタック（予定）

**推奨パッケージ:**
- `vite-plugin-pwa`: Viteと統合されたPWAプラグイン
- `workbox-window`: Service Workerの管理

**実装内容:**
- Web App Manifest（アプリ名、アイコン、テーマカラーなど）
- Service Worker（キャッシュ戦略、オフライン対応）
- インストールプロンプト
- オフライン時のフォールバックUI

### 移行の優先順位

1. **高優先度（短期）**
   - データフェッチング層の抽象化
   - エラーハンドリングの統一
   - 型安全性の向上

2. **中優先度（中期）**
   - React Routerの導入
   - React Queryの導入
   - テスト戦略の導入

3. **低優先度（長期）**
   - 状態管理ライブラリの導入
   - PWAの実装
   - ネイティブアプリの検討

### 技術的負債の管理

現在のコードベースで注意すべき点：
- デモモードと本番モードの分岐ロジックが複雑
- コンポーネントのサイズが大きい（PostBoardScreen.tsxが840行）
- 重複したデータフェッチングロジック
- エラーハンドリングの不統一

これらは段階的にリファクタリングし、上記の技術パターンの進化と合わせて改善していく必要があります。

---

## 開発フロー・運用方法

### Issue駆動開発（Issue-Driven Development）

本プロジェクトは**Issue駆動開発**を採用しており、すべての開発作業はGitHub Issueから始まります。

#### Issue駆動開発の原則

1. **すべての作業はIssueから始まる**
   - 新機能の追加
   - バグ修正
   - リファクタリング
   - ドキュメント更新
   - 技術的負債の解消

2. **Issueの種類**
   - **Feature**: 新機能の追加
   - **Bug**: バグ修正
   - **Refactor**: リファクタリング
   - **Documentation**: ドキュメント更新
   - **Enhancement**: 既存機能の改善

3. **Issueの管理**
   - GitHub Projectsで管理
   - 優先順位とステータスを明確化
   - ラベルで分類

### PRベースの開発フロー

各Issueは**1つのPull Request（PR）**として実装され、レビューとマージのプロセスを経ます。

#### ブランチ戦略

```
main (本番環境)
  ↑
develop (開発環境)
  ↑
feature/issue-[番号] (機能開発ブランチ)
```

- **main**: 本番環境にデプロイされる安定したコード
- **develop**: 開発環境の統合ブランチ
- **feature/issue-[番号]**: 各Issueに対応する機能開発ブランチ

#### ブランチ命名規則

```
feature/issue-1
feature/issue-42
bugfix/issue-15
refactor/issue-30
```

- プレフィックス: `feature/`, `bugfix/`, `refactor/`, `docs/`など
- Issue番号を必ず含める

#### 開発フローの詳細ステップ

**1. Issueの選択**
- GitHub Projectsから取り組むIssueを選択
- Issueの内容と要件を確認

**2. ブランチの作成**
```bash
# developブランチにいることを確認
git branch

# developブランチに切り替え（必要に応じて）
git checkout develop

# 最新の変更を取得
git pull origin develop

# 新しいブランチを作成
git checkout -b feature/issue-1
```

**3. 開発作業**
- Issueの要件に従って実装
- コミットメッセージは簡潔に変更内容を記述
- 関連するIssue番号をコミットメッセージに含める

**4. コミットとプッシュ**
```bash
# 変更をステージング
git add .

# コミット（Issue番号を含める）
git commit -m "feat: ユーザー認証機能の実装 (#1)"

# リモートにプッシュ
git push origin feature/issue-1
```

**5. Pull Requestの作成**
- GitHubのPull Requestsタブから新規PRを作成
- **Base**: `develop`
- **Compare**: `feature/issue-1`
- PRテンプレートに従って記入
  - 概要
  - 関連Issue（`#1`のようにリンク）
  - 変更点（スクリーンショットや動画を含める）

**6. コードレビュー**
- レビュアー（田中または藺牟田）にレビュー依頼
- フィードバックに基づいて修正
- 修正後、再度レビュー依頼

**7. マージ**
- レビュー承認後、`develop`ブランチにマージ
- マージ後、featureブランチは削除

**8. デプロイ**
- `develop`ブランチの変更が安定したら`main`にマージ
- `main`ブランチから本番環境にデプロイ

### PRテンプレート

PR作成時は以下のテンプレートを使用：

```markdown
## 概要
<!-- このセクションでは、この PR の目的と概要を簡潔に説明してください。 -->

## 関連 Issue
<!-- このセクションでは、この PR が関連する Issue やタスクをリンクしてください。 -->
- 関連 Issue: #123

## 変更点
<!-- このセクションでは、具体的な変更点や修正箇所をスクリーンショットや動画で示してください。 -->
- 変更点 1
- 変更点 2
- 変更点 3
```

### コミットメッセージの規約

コミットメッセージは以下の形式を推奨：

```
<type>: <subject> (#<issue-number>)

<body>
```

**Typeの種類:**
- `feat`: 新機能
- `fix`: バグ修正
- `refactor`: リファクタリング
- `docs`: ドキュメント更新
- `style`: コードスタイルの変更
- `test`: テストの追加・修正
- `chore`: ビルドプロセスやツールの変更

**例:**
```
feat: ユーザー認証機能の実装 (#1)

- Supabase認証の統合
- ログイン・ログアウト機能の追加
- セッション管理の実装
```

### コードレビューのチェックリスト

レビュアーは以下を確認：

- [ ] コードがIssueの要件を満たしているか
- [ ] コードスタイルが統一されているか
- [ ] テストが適切に追加されているか（該当する場合）
- [ ] ドキュメントが更新されているか（該当する場合）
- [ ] パフォーマンスに問題がないか
- [ ] セキュリティ上の問題がないか
- [ ] エラーハンドリングが適切か
- [ ] 型安全性が保たれているか（TypeScript）

### IssueとPRの連携

**Issueの自動クローズ:**
PRの説明やコミットメッセージに `Closes #1` または `Fixes #1` を含めることで、PRマージ時に自動的にIssueがクローズされます。

**例:**
```markdown
## 関連 Issue
- Closes #1
```

### 開発環境のセットアップ

**初回セットアップ:**
```bash
# リポジトリをクローン
git clone https://github.com/tanaka-0224/Iverse2.git

# ディレクトリに移動
cd Iverse2

# developブランチに切り替え
git checkout develop

# 依存関係のインストール
npm install

# 環境変数の設定
# .envファイルを作成し、Supabaseの認証情報を設定

# 開発サーバー起動
npm run dev
```

**日常的な開発:**
```bash
# developブランチの最新を取得
git checkout develop
git pull origin develop

# 新しいブランチを作成
git checkout -b feature/issue-[番号]

# 開発作業...

# コミットとプッシュ
git add .
git commit -m "feat: 変更内容 (#番号)"
git push origin feature/issue-[番号]
```

### ベストプラクティス

1. **1つのPRは1つのIssueに対応**
   - 複数のIssueを1つのPRで対応しない
   - 大きなIssueは小さなIssueに分割

2. **小さなPRを心がける**
   - レビューしやすいサイズ（200行以下を目安）
   - 大きな変更は複数のPRに分割

3. **定期的な同期**
   - 作業開始前に`develop`ブランチを最新化
   - 長期間の作業中も定期的に同期

4. **テストの実行**
   - PR作成前にローカルでテスト
   - リンターエラーの確認（`npm run lint`）

5. **ドキュメントの更新**
   - 新機能追加時はREADMEやコメントを更新
   - API変更時は型定義を更新

### CI/CDの将来計画

現在は手動でのマージとデプロイですが、将来的には以下を導入予定：

- **GitHub Actions**による自動テスト
- 自動リンター・フォーマッターの実行
- 自動デプロイメント（`main`ブランチへのマージ時）
- プレビュー環境の自動生成（PRごと）

